{% extends 'base.html.twig' %}

{% block body %}

    <div class="container my-5 small-container">

        <h1 class="mb-5">Ma commande</h1>

        {{ form_start(deliveryForm) }}
        {{ form_row(deliveryForm.addresses) }}
        <a href="{{ path('app_account_address_form') }}" class="small">Ajouter une adresse</a>
        <hr>
        {{ form_row(deliveryForm.carrier) }}
        {{ form_end(deliveryForm) }}

    </div>

{% endblock %}
